/* import { render, screen } from '@testing-library/react';
import '@testing-library/jest-dom';
import Header from './Header';

 
describe('Header component', () => {
  test('renders header with correct links', () => {
    render(<Header)
    //render(<Header />);
    // Überprüfe, ob der Link zur Startseite angezeigt wird
    expect(screen.getByText('Einkaufsliste App')).toBeInTheDocument();
    // Überprüfe, ob der Link "Neue Liste erstellen" angezeigt wird
    expect(screen.getByText('Neue Liste erstellen')).toBeInTheDocument();
    // Überprüfe, ob der Link "Liste beitreten" angezeigt wird
    expect(screen.getByText('Liste beitreten')).toBeInTheDocument();
 
    // Überprüfe, ob der Link "Einkaufsliste anzeigen" angezeigt wird
    expect(screen.getByText('Einkaufsliste anzeigen')).toBeInTheDocument();
  });
}); */